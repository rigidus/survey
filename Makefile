all:
	echo " make it manually: "
	# $ sudo apt-get install sbcl
	# install package and library manager from https://www.quicklisp.org/beta/
	# $ sbcl
	# * (load "run.sh") ;; # run it into sbcl
	# (save-lisp-and-die "core" :toplevel #'core-main :executable t) ;; run it into sbcl
	# $ ./core
